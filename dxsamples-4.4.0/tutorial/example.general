file = external.data
grid = 5 x 5 x 5
format = ascii
interleaving = record
majority = row
header = lines 3
field = data
structure = scalar
type = float
dependency = positions
positions = 1.0, .5, 3.0, .3, 2.0, .8

end
