file = 3D2ormorevarnotseries.data
grid = 40 x 60 x 20
format = msb binary
interleaving = record-vector
majority = row
header = marker "end\n"
field = e-density, gradient, e-density_squared
structure = scalar, 3-vector, scalar
type = float, float, float
dependency = positions, positions, positions
positions = regular, regular, regular, -1, .1, -3 .1, -2 .1

end
